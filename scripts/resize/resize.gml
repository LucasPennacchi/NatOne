/// @function                 resize(size);
/// @param {real}  size     scale 0 - 100 %
/// @description              resize the caller.
function resize(size = 100){
	image_xscale = size/100;
	image_yscale = size/100;
}